== README


Ruby version: 4.2.6
Database: Postgresql

To set up locally:
  - Start Postgres
  - In the CLI:
    $ bundle
    $ rake db:create
    $ rake db:migrate
    $ rails s
  - Open browser tab, go to localhost:3000

<tt>rake doc:app</tt>.
